import Fret from './Fret'
import { Row } from 'react-bootstrap'

const Frets = ({ openFretNote, frets }) => {

}
export default function String(props) {
  const { openFretNote, frets } = props

  return <Row></Row>
}