import { Box } from 'grommet';

export interface MelodyNotesProps {
  note: string;
}
export const MelodyNotes = (props: MelodyNotesProps): JSX.Element => {
  return <Box></Box>;
};
export interface MelodySolresolProps {
  note: string;
}
export const MelodySolresol = (props: MelodySolresolProps): JSX.Element => {
  return <Box></Box>;
};
export interface MelodyControlsProps {
  note: string;
}
export const MelodyControls = (props: MelodyControlsProps): JSX.Element => {
  return <Box></Box>;
};
export interface MelodySetupProps {
  note: string;
}
export const MelodySetup = (props: MelodySetupProps): JSX.Element => {
  return <Box></Box>;
};
